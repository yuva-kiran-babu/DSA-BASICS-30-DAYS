class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        count = 0
        for num in nums:
            new = len(str(num))
            if new % 2 == 0:
                count += 1
        return count